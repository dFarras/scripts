Prepare the script and run it:
1. Create a folder called secrets in configuration folder
2. Create an aemet.py in it
3. Add a constant called api_key in it with your aemet api key
4. Run pip install requests